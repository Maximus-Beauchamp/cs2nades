import React from 'react';

const Header = () => {
  return (
    <header>
      <nav>
        <button>Editor</button>
        <button>Playbook</button>
        <button>AI IGL</button>
      </nav>
    </header>
  );
};

export default Header;